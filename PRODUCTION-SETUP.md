# SKY Coffee House — تشغيل Production

## 1) قاعدة البيانات
افتح Supabase → SQL Editor وشغّل **كل** ملف `TABLE-SESSIONS-SETUP.sql` مرة واحدة.

الملف يجهز:
- الطلبات `orders`
- جلسات الطاولات `table_sessions`
- نداء النادل `waiter_calls`
- Realtime
- عملية إرسال طلب ذرّية `place_order`
- عملية إغلاق طاولة ذرّية `close_table_session`

## 2) ربط Supabase
افتح `js/supabase-config.js` وضع:
- Project URL
- Anon / Publishable key

لا تضع `service_role` key داخل الموقع.

## 3) تشغيل الموقع
ارفع المجلد كاملاً إلى الاستضافة.

رابط الزبون يكون مثل:
`https://YOUR-DOMAIN/?table=1`

ورابط المطبخ:
`https://YOUR-DOMAIN/management/kitchen.html`

## 4) مسار العمل النهائي
1. الزبون يمسح QR/NFC الخاص بالطاولة.
2. النظام يفتح/يعيد استخدام جلسة الطاولة المفتوحة.
3. الزبون يضيف المنتجات للسلة.
4. عند الضغط على **تأكيد الطلب** يتم إرسال الطلب إلى Supabase.
5. المطبخ يستقبل الطلب مباشرة عبر Realtime، مع مزامنة احتياطية كل 5 ثوانٍ.
6. المطبخ يغيّر الحالة: جديد → قيد التحضير → جاهز → تم التسليم.
7. لا يمكن إغلاق الطاولة إذا كان عليها طلب غير مُسلّم.
8. بعد الإغلاق، أي محاولة لإرسال طلب من الجلسة القديمة تُرفض من قاعدة البيانات.
9. عند مسح QR للطاولة من جديد بعد الإغلاق، تبدأ جلسة جديدة.

## ملاحظة مهمة
حماية صفحة المطبخ الموجودة حالياً بكلمة مرور JavaScript هي حماية واجهة فقط وليست حماية أمنية حقيقية. قبل الاستخدام التجاري، يُفضّل ربط المطبخ بـ Supabase Auth وسياسات RLS خاصة بالموظفين.

# SKY Coffee House — الإصلاحات

تم إصلاح مسار الطلب بالكامل:

1. إضافة المنتج للسلة تعمل من بطاقات المنيو، السلايدر، ونافذة المنتج.
2. السلة تحفظ المنتجات والكميات بشكل صحيح.
3. زر «تأكيد الطلب» يرسل الطلب إلى جدول `orders` عند تفعيل Supabase.
4. في وضع التجربة بدون Supabase، الطلبات تتزامن بين صفحات/تبويبات نفس المتصفح عبر `localStorage` + `BroadcastChannel`.
5. المطبخ يستقبل الطلبات الجديدة ويستطيع نقلها: مستلم → قيد التحضير → جاهز → تم التسليم.
6. إدارة الطاولات تعمل أيضاً في وضع التجربة: يمكن إغلاق الطاولة بعد إنهاء جميع الطلبات.
7. بعد إغلاق الطاولة، لا يمكن إرسال طلب جديد من جلسة الطاولة القديمة.
8. عند مسح QR/فتح رابط الطاولة من جديد بعد الإغلاق، يتم إنشاء جلسة جديدة تلقائياً.
9. تم توحيد البحث عن المنتجات باستخدام ID نصي/رقمي حتى لا يفشل زر الإضافة بسبب اختلاف نوع الـ ID.

## للتشغيل الحقيقي بين هاتف الزبون وجهاز المطبخ

يجب ربط المشروع بـ Supabase. افتح `js/supabase-config.js` وضع:

- `SUPABASE_URL`
- `SUPABASE_ANON_KEY`

ثم شغّل `TABLE-SESSIONS-SETUP.sql` مرة واحدة في SQL Editor في مشروع Supabase.

بدون Supabase، وضع التجربة يعمل للتأكد من منطق السلة والطلب والمطبخ، لكن المزامنة تكون داخل نفس أصل الموقع/المتصفح وليست خدمة سحابية بين أجهزة مختلفة.
